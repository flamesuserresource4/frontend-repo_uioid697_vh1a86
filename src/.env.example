# Frontend (Vite)
# Point to your running backend URL (no trailing slash)
VITE_BACKEND_URL=http://localhost:8000

# Optional static Payment Link as a fallback if dynamic checkout is unavailable
VITE_PRO_CHECKOUT_URL=
